---
description: Ready-to-use Master Prompt for building any new standardized module.
---

# 🚀 Master Module Builder Prompt

Copy and paste the block below to start building a new feature. Just replace the text inside the brackets `[ ]`.

---

> I need to build a new **[MODULE NAME]** module. My goal for this module is: **[DESCRIBE THE GOAL/FIELDS HERE]**.
>
> Please develop this using our full universal suite:
>
> 1. Use `/create-new-module` for the initial setup.
> 2. Follow `/global-rules` and `/architecture-rules` for the structure.
> 3. Implement the internal code using our `/logic-layering-standard`.
> 4. Apply `/naming-convention-standard` for all files, folders, and variables.
> 5. Use `/error-handling-standard` for all API and service logic.
> 6. Before finishing, verify quality using `/testing-qa-standard` and `/documentation-standard`.

---

## 🛠️ How to use this:

- **Module Name**: (e.g., "Inventory", "Payroll")
- **Goal**: (e.g., "Add, edit, and delete items with SKU, Quantity, and Price fields")

When you send this prompt, I will automatically read all 8 blueprints and build your module to our exact enterprise standards.
